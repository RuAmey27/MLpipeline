name: ML Pipeline

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 0 * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m venv myenv
          source myenv/bin/activate
          pip install -r requirements.txt

      - name: Check dataset changes and retrain if needed
        run: |
          source myenv/bin/activate
          python train.py

      - name: Deploy Model to EC2
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
          EC2_USER: ${{ secrets.EC2_USER }}
          SSH_KEY: ${{ secrets.SSH_KEY }}
        run: |
          echo "-----BEGIN OPENSSH PRIVATE KEY----------END OPENSSH PRIVATE KEY-----"  > Flaskapp.pem
          chmod 600 Flaskapp.pem
          
          # Check if folder exists, if not, create it
          ssh -i "Flaskapp.pem" ubuntu@ec2-3-89-117-27.compute-1.amazonaws.com '[ -d "/home/$EC2_USER/models" ] || mkdir -p /home/$EC2_USER/models'

          # Copy model file to EC2
          scp -i Flaskapp.pem models/model_*.pkl ubuntu@ip-172-31-83-233:/home/$EC2_USER/models/

          # Restart application
          ssh -i Flaskapp.pem ubuntu@ip-172-31-83-233 "sudo systemctl restart my_flask_app"
  